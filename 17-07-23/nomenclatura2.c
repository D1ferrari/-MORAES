#include <stdio.h>      // Corrigir o código "#include"

int main(){
   int n1, n2, soma;
   printf("Digite dois numeros: ");
   scanf("%d %d", &n1, &n2);
   soma = n1 + n2;
   printf("A soma dos numeros eh %d\n", soma);
   return 0;            // Corrigir o código para "return"
}